{
  "collected_at": "2026-04-28T07:43:55Z",
  "prs": [
    {
      "number": 40871,
      "title": "[New Model][ROCm] Add AMD support for DeepSeek V4",
      "author": "whx-sjtu",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-25T09:19:59Z",
      "updated_at": "2026-04-28T07:22:29Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40871",
      "labels": [
        "documentation",
        "performance",
        "new-model",
        "rocm",
        "speculative-decoding",
        "ci/build",
        "v1",
        "tool-calling",
        "deepseek",
        "gpt-oss",
        "kv-connector",
        "nvidia"
      ],
      "draft": false,
      "body_head": "## Purpose\r\nThis PR adds support of DeepSeek V4 for AMD.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\ndocker image: rocm/vllm-dev:nightly\r\nLaunch command:\r\n```\r\nmax_num_seqs=512\r\nmax_num_batched_tokens=8192\r\ntensor_parallel_size=4\r\nexport VLLM_TORCH_PROFILER_DIR=\"/app/vllm_profile\"\r\nexport HF_HOME=/data/huggingface-cache\r\nexport VLLM_ROCM_USE_AITER=1\r\nexport VLLM_ROCM_USE_AITER_LINEAR=1\r\n\r\nMODEL=deepseek-ai/DeepSeek-V4-Flash\r\n\r\nvllm serve ${MODEL} \\\r\n    --host localhost \\\r\n    --port 8000 \\\r\n    --dtype auto \\\r\n    --tensor-parallel-size ${tensor_parallel_size} \\\r\n    --max-num-seqs ${max_num_seqs} \\\r\n    --distributed-executor-backend mp \\\r\n    --trust-remote-code \\\r\n    --profiler-config '{\"profiler\": \"torch\", \"torch_profiler_dir\": \"./vllm_profile\"}' \\\r\n    --gpu-memory-utilization 0.40 \\\r\n    --moe-backend \"triton_unfused\" \\\r\n    --enforce-eager \\\r\n    --tokenizer-mode \"deepseek_v4\" \\\r\n    --async-scheduling\r\n```\r\ncurl result:\r\n```\r\ncurl -s http://localhost:8000/v1/completions -H \"Content-Type: application/json\" -d '{\r\n  \"prompt\": \"Write me a poem about AMD and Deepseek\",\r\n  \"model\": \"/home/models/DeepSeek-V4-Flash\",\r\n  \"max_tokens\": 100,\r\n  \"temperature\": 0.0\r\n}'\r\n\r\n{\"id\":\"cmpl-9d5fd5d35a7a34d8\",\"object\":\"text_completion\",\"created\":1777113241,\"model\":\"/home/models/DeepSeek-V4-Flash\",\"choices\":[{\"index\":0,\"text\":\"\\\", \\\"role\\\": \\\"user\\\" }, { \\\"content\\\": \\\"Here's a poem about AMD and DeepSeek:\\\\n\\\\n**The Silicon Spark**\\\\n\\\\nIn Santa Clara's sunlit halls, where silicon dreams are spun,\\\\nA company called AMD, its race had just begun.\\\\nWith Ryzen's multi-core design, they challenged Intel's throne,\\\\nAnd built the chips that power worlds, in code and flesh and bone.\\\\n\\\\nBut far across the ocean's span\",\"logprobs\":null,\"finish_reason\":\"length\",\"stop_reason\":null,\"token_ids\":null,\"prompt_logprobs\":null,\"prompt_token_ids\":null}],\"service_tier\":null,\"system_fingerprint\":null,\"usage\":{\"prompt_tokens\":9,\"total_tokens\":109,\"completion_tokens\":100,\"prompt_tokens_details\":null},\"k"
    },
    {
      "number": 40009,
      "title": "[ROCm] Fuse Q/K RMSNorm in MLA attention via AITER `fused_qk_rmsnorm`",
      "author": "xaguilar-amd",
      "state": "closed",
      "merged": false,
      "created_at": "2026-04-16T12:11:59Z",
      "updated_at": "2026-04-28T07:02:28Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40009",
      "labels": [
        "rocm"
      ],
      "draft": false,
      "body_head": "## Summary\r\n\r\nFuse the two separate RMSNorm kernel launches (`q_a_layernorm` and `kv_a_layernorm`) in Multi-head Latent Attention (MLA) into a single AITER `fused_qk_rmsnorm` kernel on ROCm. This eliminates one GPU kernel launch per transformer layer per decode step for DeepSeek V2/V3/Kimi models.\r\n\r\n## Motivation\r\n\r\nDuring decode on MI3XX with DeepSeek V3 / Kimi-K2.5-MXFP4, the MLA attention block performs two consecutive RMSNorm operations on the compressed latent tensors:\r\n\r\n1. `q_a_layernorm(q_c)` -- normalizes the compressed query latent (dim=512)\r\n2. `kv_a_layernorm(kv_c)` -- normalizes the compressed key-value latent (dim=1536)\r\n\r\nEach is a separate GPU kernel launch. At decode time, where per-kernel compute is ~4-5us but kernel launch overhead is significant, eliminating launches directly improves latency.\r\n\r\nAITER provides a `fused_qk_rmsnorm` kernel that fuses both operations into a single launch using a 2D grid where `blockIdx.y` selects the Q (y=0) or K (y=1) normalization path, running both in parallel across different CUs.\r\n\r\n**Impact**: Eliminates 1 kernel launch per layer per step. With 61 layers in DeepSeek V3, this saves 61 kernel launches per decode step.\r\n\r\n\r\n## Design decisions\r\n\r\n- **Model-level change, not a fusion pass**: The existing `QKNormRoPEFusionPass` targets a different pattern (standard QKV per-head norm + RoPE). The MLA pattern has different tensor shapes (latent-space norms on dims 512 and 1536, not per-head norms) and the two norms are separated from RoPE by a GEMM (`q_b_proj`). A direct model-level change in `mla.py` is more reliable than a graph pattern match.\r\n- **Guarded behind AITER availability**: The fused path only activates when all conditions are met: ROCm platform, AITER installed, AITER rmsnorm enabled via env var. All other platforms and configurations use the existing two-norm path with zero behavioral change.\r\n- **AITER's built-in fallback**: The AITER `fused_qk_rmsnorm` function automatically falls back to two separ"
    },
    {
      "number": 40848,
      "title": "[Frontend][RFC] Rust front-end integration",
      "author": "njhill",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-24T23:53:16Z",
      "updated_at": "2026-04-28T06:48:06Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40848",
      "labels": [
        "documentation",
        "rocm",
        "frontend",
        "ci/build",
        "v1",
        "cpu",
        "nvidia"
      ],
      "draft": false,
      "body_head": "See corresponding RFC for introducing a rust-based alternative front-end process in vLLM https://github.com/vllm-project/vllm/issues/40846.\r\n\r\nFor now we have staged the poc implementation in https://github.com/Inferact/vllm-frontend-rs. This PR contains the logic to integrate it into vLLM.\r\n\r\nYou can try it out by building this branch:\r\n\r\n```shell\r\n# Full install\r\nuv pip install .\r\n\r\n# Or use pre-compiled wheel\r\nVLLM_USE_PRECOMPILED=1 uv pip install --editable .\r\n./build_rust.sh\r\n\r\n# Run it\r\nVLLM_USE_RUST_FRONTEND=1 vllm serve ...\r\n```\r\n\r\nCo-authored with @BugenZhao "
    },
    {
      "number": 38476,
      "title": "[WIP] Add TRITON_MLA_SPARSE backend for SM80 sparse MLA support",
      "author": "haosdent",
      "state": "open",
      "merged": false,
      "created_at": "2026-03-29T15:23:23Z",
      "updated_at": "2026-04-28T06:47:21Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/38476",
      "labels": [
        "documentation",
        "rocm",
        "intel-gpu",
        "v1",
        "nvidia"
      ],
      "draft": true,
      "body_head": "## Purpose\n\nCloses #38006. Enables sparse MLA models (GLM-5, DeepSeek-V3.2) on SM80 (A100/A800) and SM121 (GB10/DGX Spark), where DeepGEMM / FlashMLA-Sparse / FlashInfer-MLA-Sparse are unavailable.\n\n## Changes\n\n1. **Dispatch guard.** `is_deep_gemm_supported()` (SM90+ check) replaces `has_deep_gemm()` in `sparse_attn_indexer.py` / `indexer.py`. Stops DeepGEMM SM90+ kernels from being invoked on SM80/SM121.\n2. **Triton `fp8_mqa_logits` for the indexer.** `mqa_logits_triton.py` reproduces DeepGEMM's prefill + paged MQA logits using a uint8/int bit-trick (`_decode_e4m3fn`) so the kernel compiles on SM80 without `tl.float8e4nv`.\n3. **`TRITON_MLA_SPARSE` backend.** `triton_sparse_mla_kernel.py` adds a split-KV decode path with N-way online-softmax merge, plus a single-pass fast path. Autotune is warmed at backend init. Masked-out sentinel is `-1e30` (not `-inf`) to avoid `NaN` from `(-inf) \u2212 (-inf)` on short prefills.\n4. **Cudagraph support.** `TritonMLASparseMetadataBuilder` advertises `AttentionCGSupport.UNIFORM_BATCH`; flips A100 TP=8 back to `FULL_AND_PIECEWISE`.\n5. **Persistent topk race fix** (`csrc/topk.cu`, `persistent_topk.cuh`). Workspace `cudaMemsetAsync` before each launch. The kernel's multi-CTA radix barrier was being initialized in CTA 0 behind a CTA-local `__syncthreads`, which doesn't grid-sync; with workspace reused across launches, CTAs 1..N-1 incremented stale counters before CTA 0's init wrote 0 \u2014 deadlocked decode at runtime `seq_len > 32768` (NCCL `_ALLGATHER_BASE` watchdog).\n6. **MXFP4 link stubs.** `mxfp4_experts_quant` / `silu_and_mul_mxfp4_experts_quant` stubs in `nvfp4_quant_entry.cu`. `mxfp4_experts_quant.cu` is gated to SM 10.x in CMake but `torch_bindings.cpp` references the symbols unconditionally, breaking source builds on SM 8.x. `VLLM_USE_PRECOMPILED=1` wheels aren't affected \u2014 upstream CI builds with all archs in `CUDA_ARCHS`, so the precompiled .so always carries the symbol.\n\n## Benchmarks\n\n8\u00d7A100 SXM TP=8, `lukealonso/GLM-5.1-NVFP4`, "
    },
    {
      "number": 41016,
      "title": "[Bugfix][ROCm][AITER MLA] Size persistent metadata buffers off max(max_num_seqs, max_cudagraph_capture_size)",
      "author": "jin-amd",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-27T14:00:52Z",
      "updated_at": "2026-04-28T06:45:46Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/41016",
      "labels": [
        "bug",
        "rocm",
        "v1",
        "nvidia"
      ],
      "draft": false,
      "body_head": "The AITER-MLA persistent scheduling buffers were sized off scheduler_config.max_num_seqs, but the kernel runs under cudagraphs at sizes up to compilation_config.max_cudagraph_capture_size. When the in-flight batch exceeds max_num_seqs,  the kernel walks past the end of the buffers (gibberish under fast_mode=True, GPU fault under fast_mode=False). Size off max(max_num_seqs, max_cudagraph_capture_size).\r\n\r\n\r\n\r\n## Purpose\r\n\r\n`AiterMLAMetadataBuilder.__init__` sizes the AITER persistent-MLA scheduling buffers (`work_meta_data`, `work_indptr`, `work_info_set`, `reduce_indptr`, `reduce_final_map`, `reduce_partial_map`, plus `paged_kv_*` / `qo_indptr` under full cudagraphs) off `scheduler_config.max_num_seqs`. But the kernel runs under cudagraph capture at batch sizes up to `compilation_config.max_cudagraph_capture_size`, which chunked prefill / mixed batches push above `max_num_seqs` (e.g. with `--max-num-seqs 16` the capture schedule is `[1,2,4,8,16,24,32]`, so `max_cudagraph_capture_size = 32`). Once the in-flight batch crosses that threshold the kernel walks past the end of the buffers \u2014 silent KV corruption (gibberish) under `fast_mode=True`, GPU memory access fault under `fast_mode=False`.\r\n\r\nSizing off `max(max_num_seqs, max_cudagraph_capture_size)` instead is a one-line change, cannot shrink any allocation, and grows the persistent metadata by \u22642\u00d7 (a few MB of int32 indptrs per rank, dominated by `paged_kv_indices`).\r\n\r\nBug introduced by #36574. The same idiom is copied into open PRs #40889, #40871 and #29146, which will pick up the fix on rebase.\r\n\r\n## Test Plan\r\n\r\nReproduce on DeepSeek-V3.2 TP=4 on MI300X with the smallest config that triggers the bug:\r\n\r\n```bash\r\ndocker run -d --name dsv32-tp4-repro \\\r\n  --device /dev/dri --device /dev/kfd \\\r\n  --network host --ipc host --shm-size 32g \\\r\n  --group-add video --security-opt seccomp=unconfined \\\r\n  -v /path/to/hf_cache:/root/.cache/huggingface/hub \\\r\n  -e VLLM_ROCM_USE_AITER=1 \\\r\n  --entrypoint vllm \\\r\n  vllm/vllm-"
    },
    {
      "number": 41099,
      "title": "[ROCm] Add AITER RoPE+KV cache and dual RMSNorm fusion for MLA",
      "author": "khairulkabir1661",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-28T05:42:57Z",
      "updated_at": "2026-04-28T06:45:34Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/41099",
      "labels": [
        "rocm"
      ],
      "draft": false,
      "body_head": "## Summary\r\n\r\nThis PR adds AITER's fused RoPE + KV cache kernels for DeepSeek MLA models on AMD ROCm. Combines RoPE application, KV cache writes, and dual RMSNorm into optimized fusion paths.\r\n\r\n### Key Features\r\n- **Fused Dual RMSNorm**: Uses AITER's `fused_qk_rmsnorm` \r\n- **Fused RoPE + KV Cache (Prefill)**: Single kernel for RoPE and cache write\r\n- **Fused BMM + RoPE + KV Cache (Decode)**: Combines matmul, RoPE, and cache write\r\n- **FP4 and FP8 Support**: Separate optimized kernels for both quantization formats\r\n\r\n### AITER Kernels Used\r\n- `fused_qk_rmsnorm` - Dual RMSNorm fusion\r\n- `fused_qk_rope_cat_and_cache_mla` - RoPE + KV cache (prefill)\r\n- `fused_fp4_bmm_rope_cat_and_cache_mla` - FP4 BMM + RoPE + KV cache (decode)\r\n- `fused_fp8_bmm_rope_cat_and_cache_mla` - FP8 BMM + RoPE + KV cache (decode)\r\n\r\n### Testing\r\n\r\nVerified on GSM8K benchmark with DeepSeek-V3:\r\n```bash\r\nexport VLLM_ROCM_USE_AITER=1\r\nexport VLLM_USE_AITER_FUSED=1\r\nexport HIP_VISIBLE_DEVICES=0,1,2,3,4,5,6,7\r\n\r\nlm_eval \\\r\n  --model vllm \\\r\n  --model_args \"pretrained=deepseek-ai/DeepSeek-V3,tensor_parallel_size=8,max_model_len=8192,max_num_batched_tokens=131072,gpu_memory_utilization=0.9,dtype=bfloat16,kv_cache_dtype=fp8,trust_remote_code=true\" \\\r\n  --tasks gsm8k \\\r\n  --num_fewshot 5 \\\r\n  --batch_size auto \\\r\n  --limit 500\r\n```\r\n\r\n|Tasks   |Version|     Filter              |n-shot  |  Metric          |     |Value|      |Stderr |\r\n|-------|--------|----------------|--------|-------------|---|------|---|----------|\r\n|gsm8k|      3      |flexible-extract |     5.      |exact_match|\u2191  |0.934|\u00b1    |0.0111|\r\n|            |              |strict-match     |     5.       |exact_match|\u2191  |0.934|\u00b1    |0.0111|\r\n\r\n\r\n### Performance\r\n\r\n***Output Token Throughput (tok/s)***\r\n\r\n| Version                |    1 RPS |    3 RPS |   10 RPS |   20 RPS |   50 RPS |  inf RPS |\r\n|------------------------|----------|----------|----------|----------|----------|----------|\r\n| vLLM Main              |   944.14 |  2172.84 |  357"
    },
    {
      "number": 41101,
      "title": "[ROCm] Add unified AITER RoPE+KV cache and dual RMSNorm fusion for MLA",
      "author": "khairulkabir1661",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-28T05:57:47Z",
      "updated_at": "2026-04-28T06:40:28Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/41101",
      "labels": [
        "rocm"
      ],
      "draft": false,
      "body_head": "## Summary\r\n\r\nThis PR adds AITER's fused RoPE + KV cache kernels for DeepSeek MLA models on AMD ROCm. Uses a simplified unified fusion approach with dual RMSNorm optimization.\r\n\r\n### Key Features\r\n- **Fused Dual RMSNorm**: Uses AITER's `fused_qk_rmsnorm`\r\n- **Unified RoPE + KV Cache Fusion**: Single kernel for both prefill and decode\r\n\r\n### AITER Kernels Used\r\n- `fused_qk_rmsnorm` - Dual RMSNorm fusion\r\n- `fused_qk_rope_cat_and_cache_mla` - Unified RoPE + KV cache (prefill & decode)\r\n\r\n## Testing\r\n\r\nVerified on GSM8K benchmark with DeepSeek-V3:\r\n```bash\r\nexport VLLM_ROCM_USE_AITER=1\r\nexport VLLM_USE_AITER_FUSED=1\r\nexport HIP_VISIBLE_DEVICES=0,1,2,3,4,5,6,7\r\n\r\nlm_eval \\\r\n  --model vllm \\\r\n  --model_args \"pretrained=deepseek-ai/DeepSeek-V3,tensor_parallel_size=8,max_model_len=8192,max_num_batched_tokens=131072,gpu_memory_utilization=0.9,dtype=bfloat16,kv_cache_dtype=fp8,trust_remote_code=true\" \\\r\n  --tasks gsm8k \\\r\n  --num_fewshot 5 \\\r\n  --batch_size auto \\\r\n  --limit 500\r\n```\r\n\r\n|Tasks   |Version|     Filter              |n-shot  |  Metric          |     |Value|      |Stderr |\r\n|-------|--------|----------------|--------|-------------|---|------|---|----------|\r\n|gsm8k|      3      |flexible-extract |     5.      |exact_match|\u2191  | 0.93|\u00b1    |0.0114|\r\n|            |              |strict-match     |     5.       |exact_match|\u2191  | 0.93|\u00b1    |0.0114|\r\n\r\n### Performance\r\n\r\n***Output Token Throughput (tok/s)***\r\n\r\n| Version                      |    1 RPS |    3 RPS |   10 RPS |   20 RPS |   50 RPS |  inf RPS |\r\n|------------------------------|----------|----------|----------|----------|----------|----------|\r\n| vLLM Main                    |   944.14 |  2172.84 |  3574.87 |  4160.66 |  4663.24 |  4946.94 |\r\n| RMSNorm + KV + Sep BMM       |   938.73 |  2125.74 |  3500.73 |  4104.38 |  4609.44 |  4896.03 |\r\n\r\n***Total Token Throughput (tok/s)***\r\n\r\n| Version                      |    1 RPS |    3 RPS |   10 RPS |   20 RPS |   50 RPS |  inf RPS |\r\n|--------------------------"
    },
    {
      "number": 40796,
      "title": "[Bugfix][Gemma 4] Clamp soft-token estimate to max_soft_tokens",
      "author": "hnt2601",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-24T10:00:53Z",
      "updated_at": "2026-04-28T06:15:53Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40796",
      "labels": [
        "bug",
        "documentation",
        "performance",
        "rocm",
        "frontend",
        "ci/build",
        "v1",
        "multi-modality",
        "tool-calling",
        "cpu",
        "kv-connector",
        "nvidia",
        "mistral"
      ],
      "draft": false,
      "body_head": "## Summary\r\n\r\nFor extreme-aspect-ratio images (e.g. `3x900`), the prompt-side `Gemma4ProcessingInfo._compute_num_soft_tokens` returned more soft tokens than the HF Gemma 4 image processor's vision tower actually emits (which is capped at `max_soft_tokens`). The mismatch crashed `_merge_multimodal_embeddings` mid-forward and propagated `ValueError` out of `EngineCore.step()` with:\r\n\r\n```\r\nAttempted to assign 280 multimodal tokens to 289 placeholders\r\n```\r\n\r\nThis PR fixes the bug by clamping the prompt-side estimator to `max_soft_tokens` so the placeholder count always matches what the encoder will emit, and adds a small typed-exception layer in the Gemma 4 model so a future engine-layer change can classify this failure mode without parsing free-form `ValueError` messages.\r\n\r\n## Root cause\r\n\r\n`Gemma4ProcessingInfo._compute_num_soft_tokens` computes target H/W via `max(unit, floor(dim * scale / unit) * unit)`. For very thin or very tall images the `max(unit, \u2026)` floor clamps one dimension up to `unit`, while the other scales freely. After dividing by `patch_size**2 * pooling_kernel_size**2`, the result can exceed `max_soft_tokens`.\r\n\r\nConcrete repro for `image_height=3, image_width=900, max_soft_tokens=280, patch_size=14, pooling_kernel_size=2`:\r\n\r\n- `scale \u2248 9.02`\r\n- `target_h = 28` (floor would give 0; `max(unit=28, \u2026)` lifts it)\r\n- `target_w = 8092`\r\n- `num_patches = (28 // 14) * (8092 // 14) = 1156`\r\n- `soft_tokens = 1156 // 4 = 289`\r\n\r\nThe HF Gemma 4 image processor caps its vision-tower output at `max_soft_tokens = 280`, so the prompt has 289 image placeholders but only 280 embeddings to fill them.\r\n\r\n## Changes\r\n\r\n### 1. Clamp at the prompt-side estimator (the fix)\r\n\r\n`Gemma4ProcessingInfo._compute_num_soft_tokens` now returns `min(num_patches // (pooling_kernel_size**2), max_soft_tokens)`. Strict tightening of an existing upper bound: any caller that previously got a value \u2264 `max_soft_tokens` is unaffected; any caller that got a value > `max_soft_tokens` was al"
    }
  ]
}