{
  "collected_at": "2026-04-27T21:44:41Z",
  "prs": [
    {
      "number": 37249,
      "title": "[MoE] Introduce Fp8PrepMixin and class-based dispatch for DeepGemm Fp8",
      "author": "yzong-rh",
      "state": "open",
      "merged": false,
      "created_at": "2026-03-17T02:50:14Z",
      "updated_at": "2026-04-27T21:31:09Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/37249",
      "labels": [
        "documentation",
        "performance",
        "new-model",
        "rocm",
        "structured-output",
        "frontend",
        "intel-gpu",
        "speculative-decoding",
        "ready",
        "ci/build",
        "v1",
        "multi-modality",
        "tool-calling",
        "llama",
        "qwen",
        "deepseek",
        "cpu",
        "gpt-oss",
        "kv-connector",
        "nvidia"
      ],
      "draft": false,
      "body_head": "## Purpose\r\n\r\nvLLM's fused MoE (Mixture-of-Experts) layer uses a three-stage pipeline to set up quantized expert kernels at model-load time:\r\n- **MoEMethod** (e.g. `Fp8MoEMethod`, `CompressedTensorsW8A8Fp8MoEMethod`, `ModelOptFp8MoEMethod`) \u2014 the quantization strategy. It registers weight/scale parameters and orchestrates the load-time pipeline (weight prep \u2192 quant config \u2192 kernel creation)\r\n- **Oracle** (`oracle/fp8.py`) \u2014 the backend-selection layer. It chooses which GPU kernel implementation (DeepGemm, Triton, Marlin, etc.) to use, then calls helper functions to prepare weights and build a quant config for that kernel.\r\n  - **Weight prep** (`convert_to_fp8_moe_kernel_format`) \u2014 reshapes/reorders checkpoint weights into the layout required by the selected kernel backend.\r\n  - **Quant config** (`FusedMoEQuantConfig` via `make_fp8_moe_quant_config`) \u2014 a small descriptor object (dtype, block shape, per-tensor vs. per-channel) that tells the kernel how the weights are quantized.\r\n\r\n### Problem\r\n\r\nThe oracle uses long if/elif chains keyed on the backend enum to dispatch weight preparation and quant-config creation. Each MoEMethod independently threads many individual tensors and scales through these helpers. This scatters backend-specific logic across the oracle rather than colocating it with the expert classes that own it.\r\n\r\n### Solution\r\n\r\nIntroduce **`Fp8MoEPrepMixin`** \u2014 a mixin with two classmethods:\r\n- `prepare_fp8_weights(cls, layer, w13, w2, w13_scale, w2_scale, block_shape)` \u2014 default no-op that returns the weights/scales unchanged.\r\n- `make_fp8_quant_config(cls, w13_scale, w2_scale, ...)` \u2014 default implementation that builds a standard `fp8_w8a8` quant config.\r\n\r\nExpert classes inherit this mixin and override the classmethods when they need custom behaviour. For DeepGemm, `DeepGemmFp8PrepMixin` overrides `prepare_fp8_weights` to call `prepare_fp8_moe_layer_for_deepgemm`. Both `TritonOrDeepGemmExperts` and `BatchedDeepGemmExperts` inherit it.\r\n\r\nThe oracle di"
    },
    {
      "number": 39527,
      "title": "[Model][Hardware][AMD][Kernel]: Enable e2e QK Norm + RoPE + KV Cache runtime fusion for Qwen3-30B-A3B on ROCM_ATTN, ROCM_AITER_FA, and ROCM_AITER_UNIFIED_ATTN",
      "author": "jhu960213",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-10T19:18:03Z",
      "updated_at": "2026-04-27T21:16:31Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/39527",
      "labels": [
        "rocm",
        "ready",
        "v1",
        "qwen"
      ],
      "draft": false,
      "body_head": "## Purpose\r\n\r\n- Enable e2e runtime QK Norm + RoPE + KV Cache fusion for Qwen3-30B. At compile time, we swap the aforementioned fusion pattern & fuse with aiter's high-performance HIP kernel `fused_qk_norm_rope_cache_pts_quant_shuffle ` for all decoder layers that satisfy a preset token compile range guarded by `rope_kvcache_fusion_max_token_num`. \r\n- This kernel supports writing KV Cache in both shuffle and non-shuffle layouts. ROCM_AITER_FA and ROCM_AITER_UNIFIED_ATTN require the fused kernel to write in non-shuffle mode, as this corresponds to the flash layout expected by FA variants. \r\n- As for ROCM_ATTN, modifications were needed on both the prefill and decode path kernels (prefill Triton, decode Triton 2D for non-power-of-2 block sizes, and decode paged attention ll4 GFX9 variants) so that they can read the value cache in the interleaved mode, which the fused kernel writes in when shuffle=True. The key cache read logic is untouched as the fused kernel already writes in the correct format, which the prefill and decode paths expect.\r\n- When shuffle=True, the fused kernel writes K with shape (blocks, heads, dim/x, block_size, x) and V with shape (blocks, heads, block_size/x, dim, x). Decode paths already natively read K in the same format, but it currently hardcodes reading of the V cache with an expected shape of (blocks, heads, dims, block_size).\r\n\r\n## Test Plan\r\n\r\nTo test this fusion out, please pass the attached toggle through the pass config under the `--compilation-config` when launching with `vllm serve`:\r\n\r\n`'pass_config': {\r\n        'fuse_qk_norm_rope_kvcache': True,\r\n        'rope_kvcache_fusion_max_token_num': 256\r\n}`\r\n\r\nFor example, something like this:\r\n\r\n`VLLM_ROCM_USE_AITER=1 \\\r\nvllm serve /path-to-your/Qwen3-30B-A3B \\\r\n  --host 0.0.0.0 \\\r\n  --port 8080 \\\r\n  --attention-backend ROCM_ATTN \\\r\n  --no-enable-prefix-caching \\\r\n  -O1 \\\r\n  --compilation-config '{\r\n    \"pass_config\": {\r\n      \"fuse_qk_norm_rope_kvcache\": true,\r\n      \"rope_kvcache_fusion_ma"
    },
    {
      "number": 39801,
      "title": "[ROCm][CI] Add missing quantization methods and fix online quant test failures",
      "author": "AndreasKaratzas",
      "state": "closed",
      "merged": true,
      "created_at": "2026-04-14T14:19:24Z",
      "updated_at": "2026-04-27T20:09:10Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/39801",
      "labels": [
        "rocm",
        "ready"
      ],
      "draft": false,
      "body_head": "Several quantization methods were failing on ROCm because they weren't registered in the platform's `supported_quantization` list, even though the underlying kernels already support them (either natively or through emulation fallbacks).\r\n\r\nThis adds `fp8_per_tensor`, `fp8_per_block`, `online`, `gpt_oss_mxfp4`, `mxfp8`, `modelopt`, `modelopt_mxfp8`, and `modelopt_mixed` to ROCm's supported quantization allowlist. These all have working codepaths on ROCm. FP8 variants use `ROCmFP8ScaledMMLinearKernel`, mxfp8 and modelopt_mxfp8 use `EmulationMxfp8LinearKernel` (introduced in #39205), and the online quant schemes resolve to fp8 methods that already work.\r\n\r\nAlso fixes two issues in the test infrastructure that were blocking the online quantization peak memory test on ROCm:\r\n\r\n1. The peak GPU memory log in `base_loader.py` was gated on `is_cuda()` only, so it never fired on ROCm. Changed to `is_cuda_alike()` which covers both CUDA and ROCm since `torch.accelerator.max_memory_allocated()` works on both.\r\n\r\n2. The memory thresholds in `test_online_quant_peak_mem` were hardcoded for CUDA's `fp8_e4m3fn`. ROCm on gfx950 uses `fp8_e4m3fnuz` which results in slightly higher memory usage (~7.45 GiB vs ~6.65 GiB), so the thresholds are now platform-aware.\r\n\r\ncc @kenroche "
    },
    {
      "number": 35038,
      "title": "[BugFix] Refactor add max_num_tokens_per_forward_pass to account for drafting",
      "author": "LucasWilkinson",
      "state": "closed",
      "merged": false,
      "created_at": "2026-02-22T01:04:04Z",
      "updated_at": "2026-04-27T18:19:27Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/35038",
      "labels": [
        "bug",
        "rocm",
        "speculative-decoding",
        "needs-rebase",
        "v1",
        "nvidia"
      ],
      "draft": false,
      "body_head": "## Purpose\n\nAlternative bugfix to #34671. Solves a crash of specdec on main.\n\nTo solve the consistency issue, we add a `max_num_tokens_per_forward_pass` to the VllmConfig, that accounts for drafting.\n\n## Testing\n\nTested with Qwen3-Next MTP and GSM8k repeated twice with various concurrencies. All pass with 85% accuracy, matching the non-spec baseline.\n\n```\nvllm serve Qwen/Qwen3-Next-80B-A3B-Instruct  \\\n  --tokenizer-mode auto  --gpu-memory-utilization 0.8 \\\n  --speculative-config '{\"method\": \"qwen3_next_mtp\", \"num_speculative_tokens\": 5}' \\\n  --tensor-parallel-size 2 --port 8042\n```"
    },
    {
      "number": 40997,
      "title": "Auto test target determination",
      "author": "avinashsingh77",
      "state": "closed",
      "merged": false,
      "created_at": "2026-04-27T09:44:00Z",
      "updated_at": "2026-04-27T15:01:48Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40997",
      "labels": [
        "documentation",
        "performance",
        "new-model",
        "rocm",
        "frontend",
        "intel-gpu",
        "speculative-decoding",
        "ci/build",
        "v1",
        "multi-modality",
        "tool-calling",
        "llama",
        "qwen",
        "deepseek",
        "cpu",
        "gpt-oss",
        "kv-connector",
        "nvidia",
        "mistral"
      ],
      "draft": false,
      "body_head": "\r\n## Purpose\r\nThis PR includes design and corresponding implementation of Automated test target determination using Claude Haiku\r\n## Test Plan\r\nRun a sample in `--dry-run` mode\r\n```\r\n.buildkite/scripts/select_tests.sh --dry-run upstream/main~5 2>&1\r\n```\r\nThis runs against whatever origin/main...HEAD diff exists on the current branch and prints the comment body to stdout.\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    }
  ]
}