{
  "collected_at": "2026-04-25T11:09:09Z",
  "prs": [
    {
      "number": 40338,
      "title": "[LoRA] MoE LoRA Refactor",
      "author": "jeejeelee",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-20T09:05:26Z",
      "updated_at": "2026-04-25T10:51:54Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40338",
      "labels": [
        "rocm",
        "intel-gpu",
        "ready",
        "gpt-oss",
        "nvidia"
      ],
      "draft": false,
      "body_head": "\r\n\r\n\r\n## Motivation\r\n\r\n\r\nCurrently, MoE LoRA is wired in by monkey-patching methods on the modular kernel at construction time. `FusedMoEWithLoRA._inject_lora_into_fused_moe` wraps `FusedMoEKernel.apply`, `TritonExperts.activation`, and `TritonExperts.moe_sum` with `fwd_decorator` / `act_decorator` / `moe_sum_decorator`, and smuggles tensors between them through  `moe_state_dict`. This has several concrete problems:\r\n\r\n### 1. Hacky and hard to maintain.\r\nThe LoRA contribution is hidden inside decorators on base-MoE methods. Someone reading the MoE experts code sees `self.activation(...)` and `self.moe_sum(...)` \u2014 they have no syntactic hint that these calls may actually run LoRA shrink/expand GEMMs, or that  `moe_state_dict` carries cross-call state between them. Debugging requires holding the patching order in your head.\r\n\r\n### 2. MoE changes don't see LoRA\r\n Because the LoRA path lives outside the expert `apply()` functions, any refactor of `TritonExperts` / `UnfusedOAITritonExperts` / `MarlinExperts` has to re-discover the LoRA contract (what `activation` receives, what `moe_sum` receives, what shape `intermediate_cache*` has). Every MoE-side change risks breaking LoRA  silently.\r\n\r\n### 3. Hard to extend\r\n  Adding support for new features \u2014 EP, additional quantized backends, new expert impls \u2014 means replicating or working around the decorator chain, and the state-dict plumbing assumes one specific control flow. There is no extension point for an expert that wants to apply LoRA at a different point .\r\n\r\n## Change\r\n\r\n\r\nTreat LoRA as a `first-class` concern in the modular MoE kernel rather than an external patch.\r\n\r\n### 1. `MoELoRAContext`: a single explicit payload\r\n\r\nA new `MoELoRAContext` dataclass (`vllm/lora/lora_context.py`) packages all of\r\nthe LoRA state a MoE forward pass needs:\r\n\r\n- `w13_lora_a_stacked` / `w13_lora_b_stacked` / `w2_lora_a_stacked` /\r\n  `w2_lora_b_stacked`\r\n- `adapter_enabled`, `max_loras`\r\n- routing/sharding info: `top_k`, `w13_num_slices`"
    },
    {
      "number": 40871,
      "title": "[New Model][ROCm] Add AMD support for DeepSeek V4",
      "author": "whx-sjtu",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-25T09:19:59Z",
      "updated_at": "2026-04-25T10:36:57Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40871",
      "labels": [
        "documentation",
        "performance",
        "new-model",
        "rocm",
        "speculative-decoding",
        "needs-rebase",
        "ci/build",
        "v1",
        "tool-calling",
        "deepseek",
        "gpt-oss",
        "kv-connector",
        "nvidia"
      ],
      "draft": true,
      "body_head": "## Purpose\r\nThis PR is based on #40760 and further supports DeepSeek V4 for AMD.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\nLaunch command:\r\n```\r\nmax_num_seqs=512\r\nmax_num_batched_tokens=8192\r\ntensor_parallel_size=4\r\nexport VLLM_TORCH_PROFILER_DIR=\"/app/vllm_profile\"\r\nexport HF_HOME=/data/huggingface-cache\r\nexport VLLM_ROCM_USE_AITER=1\r\nexport VLLM_ROCM_USE_AITER_LINEAR=1\r\nexport VLLM_DSV4_WO_A_FP8=0\r\n\r\nunset FLATMM_HIP_CLANG_PATH\r\nMODEL=deepseek-ai/DeepSeek-V4-Flash\r\n\r\nvllm serve ${MODEL} \\\r\n    --host localhost \\\r\n    --port 8000 \\\r\n    --dtype auto \\\r\n    --tensor-parallel-size ${tensor_parallel_size} \\\r\n    --max-num-seqs ${max_num_seqs} \\\r\n    --distributed-executor-backend mp \\\r\n    --trust-remote-code \\\r\n    --profiler-config '{\"profiler\": \"torch\", \"torch_profiler_dir\": \"./vllm_profile\"}' \\\r\n    --gpu-memory-utilization 0.40 \\\r\n    --moe-backend \"triton_unfused\" \\\r\n    --enforce-eager \\\r\n    --tokenizer-mode \"deepseek_v4\" \\\r\n    --async-scheduling\r\n```\r\ncurl result:\r\n```\r\ncurl -s http://localhost:8000/v1/completions -H \"Content-Type: application/json\" -d '{\r\n  \"prompt\": \"Write me a poem about AMD and Deepseek\",\r\n  \"model\": \"/home/models/DeepSeek-V4-Flash\",\r\n  \"max_tokens\": 100,\r\n  \"temperature\": 0.0\r\n}'\r\n\r\n{\"id\":\"cmpl-9d5fd5d35a7a34d8\",\"object\":\"text_completion\",\"created\":1777113241,\"model\":\"/home/models/DeepSeek-V4-Flash\",\"choices\":[{\"index\":0,\"text\":\"\\\", \\\"role\\\": \\\"user\\\" }, { \\\"content\\\": \\\"Here's a poem about AMD and DeepSeek:\\\\n\\\\n**The Silicon Spark**\\\\n\\\\nIn Santa Clara's sunlit halls, where silicon dreams are spun,\\\\nA company called AMD, its race had just begun.\\\\nWith Ryzen's multi-core design, they challenged Intel's throne,\\\\nAnd built the chips that power worlds, in code and flesh and bone.\\\\n\\\\nBut far across the ocean's span\",\"logprobs\":null,\"finish_reason\":\"length\",\"stop_reason\":null,\"token_ids\":null,\"prompt_logprobs\":null,\"prompt_token_ids\":null}],\"service_tier\":null,\"system_fingerprint\":null,\"usage\":{\"prompt_tokens\":9,\"total_tokens\":109,\"completion"
    },
    {
      "number": 40864,
      "title": "[New Model][ROCm] Add AMD support for DeepSeek V4",
      "author": "whx-sjtu",
      "state": "closed",
      "merged": false,
      "created_at": "2026-04-25T05:44:32Z",
      "updated_at": "2026-04-25T09:19:53Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40864",
      "labels": [
        "documentation",
        "performance",
        "new-model",
        "rocm",
        "speculative-decoding",
        "needs-rebase",
        "ci/build",
        "v1",
        "tool-calling",
        "deepseek",
        "gpt-oss",
        "kv-connector",
        "nvidia"
      ],
      "draft": true,
      "body_head": "## Purpose\r\nThis PR is based on #40760 and further supports DeepSeek V4 for AMD.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n\r\n"
    },
    {
      "number": 40796,
      "title": "[Bugfix][Gemma 4] Clamp soft-token estimate to max_soft_tokens",
      "author": "hnt2601",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-24T10:00:53Z",
      "updated_at": "2026-04-25T09:10:43Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40796",
      "labels": [
        "bug",
        "documentation",
        "performance",
        "rocm",
        "frontend",
        "ci/build",
        "v1",
        "multi-modality",
        "tool-calling",
        "cpu",
        "kv-connector",
        "nvidia",
        "mistral"
      ],
      "draft": false,
      "body_head": "## Summary\r\n\r\nFor extreme-aspect-ratio images (e.g. `3x900`), the prompt-side `Gemma4ProcessingInfo._compute_num_soft_tokens` returned more soft tokens than the HF Gemma 4 image processor's vision tower actually emits (which is capped at `max_soft_tokens`). The mismatch crashed `_merge_multimodal_embeddings` mid-forward and propagated `ValueError` out of `EngineCore.step()` with:\r\n\r\n```\r\nAttempted to assign 280 multimodal tokens to 289 placeholders\r\n```\r\n\r\nThis PR fixes the bug by clamping the prompt-side estimator to `max_soft_tokens` so the placeholder count always matches what the encoder will emit, and adds a small typed-exception layer in the Gemma 4 model so a future engine-layer change can classify this failure mode without parsing free-form `ValueError` messages.\r\n\r\n## Root cause\r\n\r\n`Gemma4ProcessingInfo._compute_num_soft_tokens` computes target H/W via `max(unit, floor(dim * scale / unit) * unit)`. For very thin or very tall images the `max(unit, \u2026)` floor clamps one dimension up to `unit`, while the other scales freely. After dividing by `patch_size**2 * pooling_kernel_size**2`, the result can exceed `max_soft_tokens`.\r\n\r\nConcrete repro for `image_height=3, image_width=900, max_soft_tokens=280, patch_size=14, pooling_kernel_size=2`:\r\n\r\n- `scale \u2248 9.02`\r\n- `target_h = 28` (floor would give 0; `max(unit=28, \u2026)` lifts it)\r\n- `target_w = 8092`\r\n- `num_patches = (28 // 14) * (8092 // 14) = 1156`\r\n- `soft_tokens = 1156 // 4 = 289`\r\n\r\nThe HF Gemma 4 image processor caps its vision-tower output at `max_soft_tokens = 280`, so the prompt has 289 image placeholders but only 280 embeddings to fill them.\r\n\r\n## Changes\r\n\r\n### 1. Clamp at the prompt-side estimator (the fix)\r\n\r\n`Gemma4ProcessingInfo._compute_num_soft_tokens` now returns `min(num_patches // (pooling_kernel_size**2), max_soft_tokens)`. Strict tightening of an existing upper bound: any caller that previously got a value \u2264 `max_soft_tokens` is unaffected; any caller that got a value > `max_soft_tokens` was al"
    },
    {
      "number": 40289,
      "title": "[ROCm][ViT] Detect Triton-AMD kernels at their new aiter location",
      "author": "Lafunamor",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-19T13:39:52Z",
      "updated_at": "2026-04-25T09:05:08Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40289",
      "labels": [
        "rocm"
      ],
      "draft": false,
      "body_head": "## Purpose\n\n`flash_attn_triton_available()` in `vllm/platforms/rocm.py` gates whether `AttentionBackendEnum.FLASH_ATTN` is offered for the ViT encoder on ROCm. The check looks for `flash_attn.flash_attn_triton_amd`, but ROCm/flash-attention commit [`3f94643`](https://github.com/ROCm/flash-attention/commit/3f94643) (2026-03-12, *\\\"[AMD] Migrate to Triton Backend to Aiter\\\"*) removed that subpackage and moved the kernels into aiter at `aiter.ops.triton._triton_kernels.flash_attn_triton_amd`.\n\nAfter that migration, `find_spec(\\\"flash_attn.flash_attn_triton_amd\\\")` returns `None` on every current ROCm wheel, so the ViT silently falls back to `TORCH_SDPA`. On gfx1151 (Strix Halo / RDNA3.5) the SDPA path produces NaN/Inf image embeddings and the LM collapses into an endless `!` stream. Other ROCm targets just lose the fast path.\n\nThis PR widens the detection to accept either the legacy subpackage or the new aiter location, restoring Triton-AMD as an available ViT backend wherever the kernels actually live.\n\n## Changes\n\n`vllm/platforms/rocm.py` \u2014 `flash_attn_triton_available()`: return `False` only when **both** import paths are missing.\n\nNo behavior change when `flash_attn.flash_attn_triton_amd` still exists (older builds). When only the aiter path exists (current builds), the function now correctly returns `True`.\n\n## Test Plan / Result\n\n- Static: verified the new check returns `True` against an aiter build where only the new path exists, and `False` when neither path exists.\n- On gfx1151, using the explicit `--mm-encoder-attn-backend TRITON_ATTN` flag (a different code path \u2014 vLLM's own `vit_triton_attn_wrapper`) I confirmed the Triton ViT kernels produce correct image embeddings on Qwen3-VL-A3B AWQ.\n\n**Shortcoming I want to be transparent about:** I have *not* been able to end-to-end verify the auto-selected `AttentionBackendEnum.FLASH_ATTN` ViT path that this PR enables. In the environment I tested, aiter's JIT-built `.so`s are not discoverable by `importlib` due to a"
    },
    {
      "number": 39168,
      "title": "[ROCm] Expanded sparse MLA support",
      "author": "ekuznetsov139",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-07T09:10:40Z",
      "updated_at": "2026-04-25T08:48:16Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/39168",
      "labels": [
        "documentation",
        "rocm",
        "ready",
        "v1"
      ],
      "draft": false,
      "body_head": "This PR:\r\n* Fixes a bug in cache_kernels.cu that resulted in incorrect results for block_stride not multiple of 16\r\n* Adds block size 16 to supported sparse MLA block sizes on ROCm\r\n* Fixes a few bugs in the Python code\r\n* Adds optimized Triton kernels with support of arbitrary block size for two of the steps\r\n* In case of rocm_fp8_paged_mqa_logits, routes to the Triton kernel by default, since AITER  is slower for block size 1 and it does not handle block size 16 correctly\r\n\r\nI've verified that, after this PR, both GLM-5-FP8 and DeepSeek-V3.2 work correctly on ROCm (MI325  / gfx942) with both --block-size 1 and --block-size 16."
    },
    {
      "number": 33761,
      "title": "fix(rocm): use correct kv_cache_layout in extend_for_sliding_window",
      "author": "gigamonkeyx",
      "state": "open",
      "merged": false,
      "created_at": "2026-02-04T04:31:45Z",
      "updated_at": "2026-04-25T07:20:38Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/33761",
      "labels": [
        "rocm",
        "v1"
      ],
      "draft": false,
      "body_head": "## Summary\n\nFixes #33123 - Non-deterministic prefix caching output on ROCm with AITER backend.\n\n## Root Cause\n\nWhen shuffle KV cache is enabled via `rocm_aiter_ops.is_shuffle_kv_cache_enabled()`, the `cp_mha_gather_cache` triton kernel must use `\"SHUFFLE\"` layout. However, `extend_for_sliding_window()` was hardcoded to use `\"NHD\"`, causing a layout mismatch when reading cached KV pairs.\n\nThis mismatch caused the triton kernel to read memory with incorrect indexing, producing garbage values that varied based on memory state - explaining the observed non-determinism.\n\n## Changes\n\n1. **Bug fix**: Use `self._kv_cache_layout_str` property instead of hardcoded `\"NHD\"` in both `extend_for_sliding_window()` and `extend_forward()` \n\n2. **Refactor**: Consolidated duplicated conditional logic into a `_kv_cache_layout_str` property to prevent similar bugs\n\n```python\n@property\ndef _kv_cache_layout_str(self) -> str:\n    \"\"\"Get the KV cache layout string based on shuffle setting.\"\"\"\n    return \"SHUFFLE\" if rocm_aiter_ops.is_shuffle_kv_cache_enabled() else \"NHD\"\n```\n\n## Testing\n\n- Syntax validation passed\n- Requires ROCm hardware testing (vLLM CI has ROCm runners)\n\n## Related\n\n- Supersedes #33759 (DCO issues with that PR)"
    },
    {
      "number": 39238,
      "title": "[ROCm][CI] Fine-tuning queues and test names",
      "author": "AndreasKaratzas",
      "state": "open",
      "merged": false,
      "created_at": "2026-04-07T22:42:17Z",
      "updated_at": "2026-04-25T07:14:41Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/39238",
      "labels": [
        "rocm",
        "ci/build"
      ],
      "draft": false,
      "body_head": "This PR makes names of tests more descriptive on upstream. Also it fixes inconsistencies in the AMD CI pipeline configuration. It ensures agent_pool suffixes match the GPU count implied by step labels and num_gpus values, and adds missing num_gpus fields where the agent pool already implies multi-GPU execution.\r\n\r\ncc @kenroche "
    },
    {
      "number": 34764,
      "title": "[Build] Update numba version to 0.63.1.",
      "author": "nascheme",
      "state": "closed",
      "merged": false,
      "created_at": "2026-02-18T02:18:45Z",
      "updated_at": "2026-04-25T05:48:07Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/34764",
      "labels": [
        "rocm",
        "ready",
        "needs-rebase",
        "ci/build",
        "cpu",
        "nvidia"
      ],
      "draft": false,
      "body_head": "## Purpose\n\nFor vLLM to support Python 3.14, we need to use a newer version of numba.  Version 0.63 of numba adds support for 3.14.\n\n## Test Plan\n\nRun ngram_proposer tests using pytest.\n\n## Test Result\n\nThe unit tests using ngram_proposer pass."
    },
    {
      "number": 38503,
      "title": "[ROCm][Engine] Fix GPU memory leaks in engine shutdown and test workaround for async KV prefix cache reset",
      "author": "AndreasKaratzas",
      "state": "closed",
      "merged": true,
      "created_at": "2026-03-30T04:39:50Z",
      "updated_at": "2026-04-25T05:25:44Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/38503",
      "labels": [
        "rocm",
        "ready",
        "ci/build",
        "v1",
        "kv-connector"
      ],
      "draft": false,
      "body_head": "When running in-process (`VLLM_ENABLE_V1_MULTIPROCESSING=0`), shutting down the engine leaked GPU memory because:\r\n\r\n- **`supports_kw` LRU cache pinned model weights**: The `@lru_cache` on `supports_kw` was keyed on bound methods (e.g. `model.forward`), which held strong references to the model instance and all its GPU tensors for the lifetime of the cache. Fixed by unwrapping bound methods to `__func__` before caching.\r\n- **`gc.freeze()` was never undone**: `EngineCore.__init__` calls `gc.freeze()` to reduce GC overhead during serving, but `shutdown()` never called `gc.unfreeze()`, making all engine objects invisible to the garbage collector.\r\n- **No explicit GPU resource teardown**: `GPUWorker.shutdown()` never freed model weights, KV caches, the rotary embedding cache (`_ROPE_DICT`), compilation contexts, or the workspace singleton.\r\n\r\n### Test workaround for prefix cache reset\r\n\r\n`reset_prefix_cache` could return `False` indefinitely when async KV store transfers (CPU offloading) were in flight, because the engine stops stepping after all requests finish and never polls the worker for completed transfers. As a temporary workaround (per reviewer suggestion), `_wait_for_prefix_cache_reset` in the test now sends dummy single-token prefills between retries to force the engine to step, which naturally drains completed transfers and frees GPU blocks.\r\n\r\n### UCX fix\r\n\r\nBump UCX branch in ROCm Dockerfile to `33b3b2a2` which fixes a memory leak in UCX when used with NIXL.\r\n\r\n## Test plan\r\n\r\n- [x] `pytest tests/v1/kv_offload/ tests/v1/kv_connector/unit/ tests/v1/core/test_scheduler.py tests/v1/core/test_prefix_caching.py tests/v1/core/test_reset_prefix_cache_e2e.py tests/v1/simple_kv_offload/test_integration.py -v`\r\n\r\n### [LEGACY] Initial Test plan and Motivation\r\n\r\n- [x] `pytest tests/v1/kv_offload/test_cpu_offloading.py::test_cpu_offloading[TRITON_ATTN-48]`\r\n\r\nMotivation: https://buildkite.com/vllm/amd-ci/builds/7062/steps/canvas?sid=019d382d-caab-452a-bf83-1c611ef498b6"
    },
    {
      "number": 40306,
      "title": "[ROCm][CI] Fix `trust_remote_code` AttributeError in EAGLE3 acceptance length test",
      "author": "AndreasKaratzas",
      "state": "closed",
      "merged": true,
      "created_at": "2026-04-19T23:44:42Z",
      "updated_at": "2026-04-25T03:13:32Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/40306",
      "labels": [
        "rocm",
        "speculative-decoding",
        "ready",
        "v1"
      ],
      "draft": false,
      "body_head": "Fixes `test_eagle3_acceptance_length` in the `tests/v1/spec_decode` test group (V1 speculative decoding tests). The test's `get_mt_bench_prompts` helper built a `SimpleNamespace` to pass into `vllm.benchmarks.datasets.get_samples`, but omitted `trust_remote_code`. `get_samples` reads `args.trust_remote_code` when constructing the HuggingFace dataset, causing every parametrization of `test_eagle3_acceptance_length` to fail with:\r\n\r\n```\r\nAttributeError: 'types.SimpleNamespace' object has no attribute 'trust_remote_code'\r\n```\r\n\r\nThis patch adds `trust_remote_code=False` to the namespace so the dataset can be constructed as intended.\r\n\r\n### Testing\r\n\r\n- [x] `pytest tests/v1/spec_decode/test_acceptance_length.py`\r\n\r\ncc @kenroche \r\n"
    },
    {
      "number": 39799,
      "title": "[ROCm][CI] Fix TestSiluMulGroupFp8QuantModel after W8A8 block linear refactor",
      "author": "AndreasKaratzas",
      "state": "closed",
      "merged": true,
      "created_at": "2026-04-14T13:41:25Z",
      "updated_at": "2026-04-25T03:13:25Z",
      "html_url": "https://github.com/vllm-project/vllm/pull/39799",
      "labels": [
        "rocm",
        "ready"
      ],
      "draft": false,
      "body_head": "The W8A8 block linear refactor (#33892) replaced `W8A8BlockFp8LinearOp` with `TestFP8Layer` in `TestSiluMulGroupFp8QuantModel` but didn't adapt the forward call: `TestFP8Layer.forward()` takes `(y, bias=None)` while the model was still passing `(y, self.w, self.wscale)`. The redundant weight/scale tensors were also leftover from `W8A8BlockFp8LinearOp` which didn't store its own weights, unlike `TestFP8Layer` which creates them internally.\r\n\r\nAdditionally, on non-fnuz ROCm (MI355) the fusion pattern expects the triton group quant op, but the kernel's `use_triton` flag was `False` for small test shapes. Fixed by wrapping `quant_fp8` to force `use_triton=True` independently of the gemm path.\r\n\r\ncc @kenroche "
    }
  ]
}